import type { RouteObject } from 'react-router';

/**
 * Showcase routes — REMOVED during cleanup.
 * These were demo pages for the design system.
 */
export const showcaseRoutes: RouteObject[] = [];
