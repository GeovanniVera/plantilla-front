import type { RouteObject } from 'react-router';

/**
 * Example routes — REMOVED during cleanup.
 * These were demo applications for the design system.
 */
export const examplesRoutes: RouteObject[] = [];
