# TODO: Configure deployment
# This workflow is a template. Uncomment and configure for your deployment target.
#
# Options:
# - Vercel: https://vercel.com/docs/github
# - Netlify: https://docs.netlify.com/configure/builds/
# - AWS Amplify: https://docs.amplify.aws/guides/hosting/
# - Custom: Add your own deployment steps below

# name: Deploy
#
# on:
#   push:
#     branches: [main]
#
# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     needs: [ci]  # Wait for CI to pass
#
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4
#
#       # TODO: Add your deployment steps here
#       # Example for Vercel:
#       # - name: Deploy to Vercel
#       #   uses: amondnet/vercel-action@v25
#       #   with:
#       #     vercel-token: ${{ secrets.VERCEL_TOKEN }}
#       #     vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
#       #     vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
#       #     vercel-args: '--prod'
#
#       # Example for Netlify:
#       # - name: Deploy to Netlify
#       #   uses: nwtgck/actions-netlify@v3
#       #   with:
#       #     publish-dir: ./dist
#       #     production-deploy: true
